# ✨ Active les événements de gestion de connexions
events {}

# 🌐 Configuration principale du serveur HTTP
http {
  # 📦 Charge les types MIME standards (pour que Nginx comprenne .js, .css, etc.)
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;

  # ⚙️ Active l'envoi efficace de fichiers
  sendfile        on;

  # 🔁 Garde les connexions HTTP ouvertes 65s pour les optimiser
  keepalive_timeout  65;

  # 🌐 Déclare les serveurs internes vers lesquels Nginx reverse proxy
  # (frontend non utilisé car on sert les builds via "root")
  upstream backend {
    server server:8080;
  }

  # ---------------------------------------------
  # 🌐 1. Compression GZIP pour tous les fichiers textes
  # ---------------------------------------------
  gzip on;  # ✅ Active la compression
  gzip_types
    text/plain
    text/css
    text/javascript
    application/javascript
    application/json
    application/xml
    application/xml+rss
    image/svg+xml;  # ⚠️ Vite produit souvent des SVG
  gzip_min_length 256;  # Ne compresse que si > 256 octets
  gzip_vary on;         # Ajoute l'en-tête Vary pour le cache/CDN
  gzip_proxied any;     # Autorise compression même derrière un proxy
  gzip_comp_level 6;    # Bon compromis entre perf & compression

  # ---------------------------------------------
  # 🧠 2. Cache côté client pour les fichiers statiques (HTML, JS, CSS, etc.)
  # ---------------------------------------------
  map $sent_http_content_type $expires {
    default                    off;
    text/html                  epoch;
    text/css                   max;
    application/javascript     max;
    application/json           max;
    image/svg+xml              max;
    font/woff2                 max;
  }

  # ---------------------------------------------
  # 🌍 3. Redirection HTTP → HTTPS pour le FRONTEND
  # ---------------------------------------------
  # ✔️ Redirection HTTP → HTTPS pour L'ATELIER PHOTO MONTPELLIER
  server {
    listen 80;
    server_name atelier-photo-montpellier.fr www.atelier-photo-montpellier.fr;

    # 🔐 Autorise Let's Encrypt à accéder au challenge
    location ^~ /.well-known/acme-challenge/ {
      root /usr/share/nginx/html;
      default_type "text/plain";
    }

    # 🔁 Redirige tout en HTTPS
    location / {
      return 301 https://$host$request_uri;
    }
  }

  # ✔️ Redirection HTTP → HTTPS pour KALEIDOPIX
  server {
    listen 80;
    server_name kaleidopix.fr www.kaleidopix.fr;

    # 🔐 Autorise Let's Encrypt à accéder au challenge
    location ^~ /.well-known/acme-challenge/ {
      root /usr/share/nginx/html;
      default_type "text/plain";
    }

    # 🔁 Redirige tout en HTTPS
    location / {
      return 301 https://$host$request_uri;
    }
  }

  # ✔️ Redirection HTTP → HTTPS pour l'API
  server {
    listen 80;
    server_name api.atelier-photo-montpellier.fr;

    location ^~ /.well-known/acme-challenge/ {
      root /usr/share/nginx/html;
      default_type "text/plain";
    }

    location / {
      return 301 https://$host$request_uri;
    }
  }

  # ---------------------------------------------
  # 🌐 4. HTTPS - FRONTEND (React build)
  # ---------------------------------------------
  # ✔️ HTTPS - FRONTEND L'ATELIER PHOTO MONTPELLIER
  server {
    listen 443 ssl;  # ✅ Active SSL
    http2 on;        # 🔁 Active HTTP/2
    server_name atelier-photo-montpellier.fr www.atelier-photo-montpellier.fr;
    ssl_certificate /etc/letsencrypt/live/atelier-photo-montpellier.fr/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/atelier-photo-montpellier.fr/privkey.pem;

    # 🔒 Durcissement TLS/HSTS
    ssl_protocols TLSv1.2 TLSv1.3;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    # 🔐 LET'S ENCRYPT — ACME CHALLENGE (HTTPS)
    # IMPORTANT : doit être AVANT "location /"
    location ^~ /.well-known/acme-challenge/ {
      root /usr/share/nginx/html;
      default_type "text/plain";
    }

    # 🧠 Active le cache (assets Vite hashés)
    # Vite génère généralement /assets/... avec un hash => on peut cache très longtemps
    location ^~ /assets/ {
      root /usr/share/nginx/html/lapm;
      expires 1y;
      add_header Cache-Control "public, immutable";
      try_files $uri =404;
    }

    # 🧠 Désactive le cache sur index.html (SPA shell)
    # Important : évite de servir un vieux index qui pointe vers de nouveaux fichiers JS/CSS
    location = /index.html {
      root /usr/share/nginx/html/lapm;
      add_header Cache-Control "no-store, no-cache, must-revalidate";
      expires off;
    }

    # 🗂️ Sert les fichiers du build React
    location / {
      root /usr/share/nginx/html/lapm;
      index index.html;

      # ✅ Sert index.html même si un chemin est inconnu (React SPA)
      try_files $uri $uri/ /index.html;
    }

    # 📡 Proxy vers l'API backend
    location /api/ {
      proxy_pass http://backend/;  # ✅ Pas de préfixe côté backend => /api/users devient /users

      # 🔁 Recommandé (Express + éventuels WebSockets)
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";

      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
  }

  # ✔️ HTTPS - FRONTEND KALEIDOPIX
  server {
    listen 443 ssl;  # ✅ Active SSL
    http2 on;        # 🔁 Active HTTP/2
    server_name kaleidopix.fr www.kaleidopix.fr;
    ssl_certificate /etc/letsencrypt/live/kaleidopix.fr/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/kaleidopix.fr/privkey.pem;

    # 🔒 Durcissement TLS/HSTS
    ssl_protocols TLSv1.2 TLSv1.3;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    # 🔐 LET'S ENCRYPT — ACME CHALLENGE (HTTPS)
    # IMPORTANT : doit être AVANT "location /"
    location ^~ /.well-known/acme-challenge/ {
      root /usr/share/nginx/html;
      default_type "text/plain";
    }

    # 🧠 Active le cache (assets Vite hashés)
    # Vite génère généralement /assets/... avec un hash => on peut cache très longtemps
    location ^~ /assets/ {
      root /usr/share/nginx/html/kaleidopix;
      expires 1y;
      add_header Cache-Control "public, immutable";
      try_files $uri =404;
    }

    # 🧠 Désactive le cache sur index.html (SPA shell)
    # Important : évite de servir un vieux index qui pointe vers de nouveaux fichiers JS/CSS
    location = /index.html {
      root /usr/share/nginx/html/kaleidopix;
      add_header Cache-Control "no-store, no-cache, must-revalidate";
      expires off;
    }

    # 🗂️ Sert les fichiers du build React
    location / {
      root /usr/share/nginx/html/kaleidopix;
      index index.html;

      # ✅ Sert index.html même si un chemin est inconnu (React SPA)
      try_files $uri $uri/ /index.html;
    }

    # 📡 Proxy vers l'API backend
    location /api/ {
      proxy_pass http://backend/;  # ✅ Pas de préfixe côté backend => /api/users devient /users

      # 🔁 Recommandé (Express + éventuels WebSockets)
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";

      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
  }

  # ---------------------------------------------
  # 🌐 5. HTTPS - BACKEND API
  # ---------------------------------------------
  server {
    listen 443 ssl;
    http2 on;
    server_name api.atelier-photo-montpellier.fr;

    ssl_certificate /etc/letsencrypt/live/api.atelier-photo-montpellier.fr/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.atelier-photo-montpellier.fr/privkey.pem;

    # 🔒 Durcissement TLS/HSTS
    ssl_protocols TLSv1.2 TLSv1.3;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    # 🔐 Autorise Let’s Encrypt à écrire ses fichiers
    # (peut être placé avant ou après, mais on le garde ici comme dans ton fichier)
    location ^~ /.well-known/acme-challenge/ {
      root /usr/share/nginx/html;
      default_type "text/plain";
    }

    # 📡 Route vers le serveur backend
    location / {
      proxy_pass http://backend/;

      # 🔁 Recommandé (Express + éventuels WebSockets)
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";

      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
  }
}
