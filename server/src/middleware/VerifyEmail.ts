import { Request, Response, NextFunction } from "express";

function VerifyEmail(res: Response, req:Request, next: NextFunction) {
    try {}
    catch (error) {}
}

export default VerifyEmail;